{
  "name": "vue-lib-template",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "docs/**",
    "packages/**"
  ],
  "scripts": {
    "lib": "yarn workspace lib",
    "dev:demo": "cd examples/demo && yarn && yarn upgrade && yarn dev",
    "build:lib": "yarn lib build",
    "publish:lib": "yarn lib publish"
  },
  "volta": {
    "node": "18.16.0",
    "yarn": "1.22.10"
  }
}
